## JS

```javascript
var content = "JavaScript describe el comportamiento de HTML.";
// Aunque puede hacer mucho más

document.body.innerHTML = content;

jQuery(document.body).text(content + ' jQuery is JavaScript')
```
